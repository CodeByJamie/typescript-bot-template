# Typescript Discord Bot Template

A clean, modular, and easy-to-use template for building Discord bots with Typescript. This project is configured with best practices in mind, including environment variable management, a structured command layout, and ready-to-go dependency handling.


## 🚀 Features
- **Quick Start**: Pre-configured setup to get your bot running in minutes.
- **Environment Management**: Uses native `bun` to keep your environmental variables secure.
- **Modular Design**: organized folder structure to keep your commands and logic clean.
- **Discord.JS**: Built on top of the powerful [discord.js](https://github.com/discordjs/discord.js/), to interaction with Discord's API seamlessly. 

## 📋 Prerequisites
Before you begin, ensure you have the following installed:

- **Bun** (Version 1.3.6 or higher recommended)
- **discord.js** (Version 14.25.1 or higher recommended)
- **chalk** (Version 5.6.2 or higher recommended)

## 🛠️ Installation
1. **Clone the repository**
   ```bash
   git clone https://github.com/CodeByJamie/typescript-bot-template.git
   cd typescript-bot-template
   ```

2. **Install Dependencies**
    ```bash
    bun install
    ```
- This ensures the node_modules is up to date with the latest dependencies.

3. **Running the process**
    ```bash
    bun index
    ```
- This will start the client up ready for development or production!